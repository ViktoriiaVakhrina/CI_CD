pipeline {
   agent any
   stages {
      stage('Environment setup') {
        steps{      
		withPythonEnv('python3') {
			sh 'pip install -r requirements.txt'
			sh 'python3 tests.py'
			}
		}
	}
	}
}